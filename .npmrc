# Keep the pnpm store inside the workspace so the broken system npm cache
# (~/.npm) and any home-dir write restrictions never affect this repo.
store-dir=.pnpm-store

# The dsh runtime installer (M2/M3) uses npm with an explicit --cache; keep
# defaults here deliberately free of registry overrides.
